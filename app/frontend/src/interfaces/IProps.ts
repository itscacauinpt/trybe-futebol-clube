interface GameProp {
  currentFilter: any;
  setCurrentFilter: any;
}

interface LDProp {
  currentFilter: any
}

interface GTProps {
  isAdm: any;
  currentFilter: any;
}

export type {
  GameProp,
  LDProp,
  GTProps,
}